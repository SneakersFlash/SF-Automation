import { PagePlaceholder } from '@/components/ui/PagePlaceholder';

export default function RevenuePage() {
  return (
    <PagePlaceholder
      section="Insights"
      title="Revenue"
      desc="Date picker, refresh, metric cards (revenue/order/AOV), Top SKU, Channel bars, tren 7 hari, Anomaly badge. Sumber Ginee (exclude cancelled/returned)."
    />
  );
}
