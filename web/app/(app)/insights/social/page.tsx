import { PagePlaceholder } from '@/components/ui/PagePlaceholder';

export default function SocialPerformancePage() {
  return (
    <PagePlaceholder
      section="Insights"
      title="Social Performance"
      desc="Brand switcher, tab platform (IG/TikTok), range, metric cards, follower trend, Top Posts grid, empty state (belum connect)."
    />
  );
}
