import { PagePlaceholder } from '@/components/ui/PagePlaceholder';

export default function CopywritingPage() {
  return (
    <PagePlaceholder
      section="Creative"
      title="Copywriting"
      desc="Toggle goal (organik/ads), Generate, blok Hooks/Caption/CTA/Hashtag (copyable), tombol Humanize semua."
    />
  );
}
