import { PagePlaceholder } from '@/components/ui/PagePlaceholder';

export default function ContentBriefPage() {
  return (
    <PagePlaceholder
      section="Creative"
      title="Content Brief"
      desc="Active Tag (subject), Brand switcher, Format chips (multi-select), tombol Generate, OutputTag per format (copyable)."
    />
  );
}
