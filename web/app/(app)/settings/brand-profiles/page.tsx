import { PagePlaceholder } from '@/components/ui/PagePlaceholder';

export default function BrandProfilesPage() {
  return (
    <PagePlaceholder
      section="Settings"
      title="Brand Profiles"
      desc="List profile, drawer form (voice/audience/platform), buat/edit (Owner & Member), set default & hapus (Owner)."
    />
  );
}
