import { PagePlaceholder } from '@/components/ui/PagePlaceholder';

export default function UsersPage() {
  return (
    <PagePlaceholder
      section="Settings"
      title="Users"
      desc="Daftar pengguna, tambah Member (email + password sementara), nonaktifkan akun. Hanya Owner."
    />
  );
}
