import { PagePlaceholder } from '@/components/ui/PagePlaceholder';

export default function SocialAccountsPage() {
  return (
    <PagePlaceholder
      section="Settings"
      title="Social Accounts"
      desc="Connect/disconnect akun IG/TikTok (OAuth) per Brand Profile. Hanya Owner (token disimpan sebagai tokenRef, tidak plain)."
    />
  );
}
