import { PagePlaceholder } from '@/components/ui/PagePlaceholder';

export default function AuditLogPage() {
  return (
    <PagePlaceholder
      section="Settings"
      title="Audit Log"
      desc="Riwayat Generation & aksi (aktor, skill/aksi, input ringkas, status, timestamp). Hanya Owner."
    />
  );
}
