import { PagePlaceholder } from '@/components/ui/PagePlaceholder';

export default function AdsEnginePage() {
  return (
    <PagePlaceholder
      section="Ops"
      title="Ads Engine"
      desc="Tab Generate (4 cards angle) | Tab Performa (tabel klasifikasi winner/ok/loser) | daftar AdAction pending + ApprovalModal. Approve/eksekusi hanya Owner."
    />
  );
}
